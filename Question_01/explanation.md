1. **whoami && groups**  
Explanation: _whoami_ to confirm my current login session and _groups_ to list all security groups my account is associated with.  


2. **pwd && ls -1**  
Explanation: _pwd_ to print the absolute path of my current directory and _ls -l_ to display files in long format.  


3. **echo "Linux user enviroinment verified" > user_info.txt**  
Explanation: _echo_ command combined with the _>_ redirection operator to create a new file named user_info.txt containing the string "Linux user...".  


4. **wc -m user_info.txt**  
Explanation: _wc_ command with _-m_ to count the exact number of characters in the user_info.txt file.  


5. **mkdir -p parent/child/g_child**  
Explanation: _mkdir --help_ command to study its documentation. _-p_ (parents) option, which allows the creation of nested subdirectories.  


6. \*\*ls ~\*\*  
Explanation: _ls ~_ command to target the home directory. The output displayed all user files and folders sorted alphabetically by default.  


7. **grep "admin" log.txt**  
Explanation: _grep_ utility to filter the log.txt file for the specific string "admin."  


8. **uname -r**  
Explanation: _uname_ command with the _-r_ flag to extract the specific kernel release version.  


9. **ping -c 4 google.com**  
Explanation: _ping_ command to send 4 ICMP packets to Google’s servers to test network connectivity.  


10. **uptime**  
Explanatino: _uptime_ command to view how long the system has been running, the number of active users, and the system load averages.

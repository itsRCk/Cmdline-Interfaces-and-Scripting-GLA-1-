1. **whoami && groups** 
Explanation: _whoami_ to confirm my current login session and _groups_ to list all security groups my account is associated with.


2. **pwd && ls -1**
Explanation: _pwd_ to print the absolute path of my current directory and _ls -l_ to display files in long format.
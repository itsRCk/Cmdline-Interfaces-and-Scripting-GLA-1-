1. `whoami && groups`  
Explanation: `whoami` to confirm my current login session and `groups` to list all security groups my account is associated with.  


2. `pwd && ls -1`  
Explanation: `pwd` to print the absolute path of my current directory and `ls -l` to display files in long format.  


3. `echo "Linux user enviroinment verified" > user_info.txt`  
Explanation: `echo` command combined with the `>` redirection operator to create a new file named user_info.txt containing the string "Linux user...".  


4. `wc -m user_info.txt`  
Explanation: `wc` command with `-m` to count the exact number of characters in the user_info.txt file.  


5. `mkdir -p parent/child/g_child`  
Explanation: `mkdir --help` command to study its documentation. `-p` (parents) option, which allows the creation of nested subdirectories.  


6. `ls ~`  
Explanation: `ls ~` command to target the home directory. The output displayed all user files and folders sorted alphabetically by default.  


7. `grep "admin" log.txt`  
Explanation: `grep` utility to filter the log.txt file for the specific string "admin."  


8. `uname -r`  
Explanation: `uname` command with the `-r` flag to extract the specific kernel release version.  


9. `ping -c 4 google.com`  
Explanation: `ping` command to send 4 ICMP packets to Google’s servers to test network connectivity.  


10. `uptime`  
Explanatino: `uptime` command to view how long the system has been running, the number of active users, and the system load averages.
